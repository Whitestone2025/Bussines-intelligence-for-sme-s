[project]
name = "human-signal"
version = "0.1.0"
description = "Evidence-first communication research system for service businesses"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
